# RESUMEN DE ACTIVIDAD - 26 DE MARZO DE 2026

**Objetivo de la sesión:** Ejecución del Roadmap Técnico Fase 1 (Seguridad y Hardening) para elevar el proyecto a nivel industrial.

### 📝 Tareas Realizadas:
1.  **Arranque de la Fase 1:** Inicio de las tareas de seguridad crítica del entorno SENTINEL.
2.  **Segmentación de Privilegios (DB):**
    *   Creación del usuario `sentinel_web` con permisos restringidos (`SELECT`, `INSERT`, `UPDATE`, `DELETE`) limitado a la base de datos `tienda_segura`.
    *   Actualización de `src/includes/conexion.php` para implementar el **Principio de Mínimo Privilegio (POLP)**.
3.  **Hardening de MariaDB (Finalizado):** Eliminación de bases de datos de test y usuarios anónimos en el motor MariaDB.
4.  **Aislamiento de Secretos (Hardening de Configuración):** Migración de credenciales de DB a un archivo externo fuera de la raíz web del proyecto (`/home/avidal/TFG/config_sentinel_db.php`) para prevenir fugas de información.
5.  **SSL End-to-End (CIFRADO INTERNO):** Configuración exitosa de certificados SSL/TLS en el servidor Apache (puerto 443) y reconfiguración del HAProxy para cifrar el tráfico dentro de la propia VPC.
6.  **Resolución de Incidencias de Red y SELinux:** Identificación y corrección de bloqueos de conexión Apache-MariaDB mediante el ajuste de privilegios de usuario (`sentinel_web@10.0.1.250`) y la habilitación del booleano `httpd_can_network_connect_db` en SELinux.
7.  **Bitácora de Desarrollo:** Inicio del archivo de incidencias técnicas (`BITACORA_ERRORES_Y_SOLUCIONES.md`) para documentar debilidades corregidas durante el desarrollo (valor añadido para la memoria final).

### 🚀 Próximos Pasos Identificados (Arrancando Fase 2):
-   Implementación de **NAT Instance** (Amazon Linux o Squid) para permitir actualizaciones seguras en la subred privada de la base de datos.
-   Configuración de **AWS Systems Manager (SSM)** para eliminar la necesidad del puerto 22 (SSH) abierto al mundo.
-   Refinar el script de Backups en S3 con rotación automática de copias.

*¡Seguimos blindando el sistema!*
