# Resumen de Sesión - 24/03/2026

## 📜 Trabajos Finalizados
1. **Infraestructura y Diagrama**:
   - Validación del diagrama de red final en formato Mermaid (`docs/diagrama_red.md`) y revisión del archivo visual SVG.
   - Refleja correctamente el flujo de tráfico actual y el stack de monitorización.

2. **Tests de Rendimiento (Stress Test)**:
   - Ejecutado `ab -n 5000 -c 50` con resultados de **43.64 RPS** y latencia media de **440ms**.
   - Capturados picos en Grafana (Apache: 15 / SQL: 30). Datos volcados en la Memoria Final.

3. **Backups en la Nube (AWS S3)**:
    *   Se ha actualizado el script `automation/scripts/backup_db.sh` con el nombre del bucket real: `tfg-sentinel-backups-alex`.
    *   Se ha verificado la instalación de **AWS CLI** en el servidor de base de datos.
    *   Identificada la restricción de **AWS Academy (Vocareum)**: requiere credenciales temporales o el uso de Roles de IAM en producción.
4.  **Implementación de "Soft Delete" (Borrado Lógico)**:
    *   Se ha verificado que la columna `activo` ya está presente en la base de datos real de AWS.
    *   Se ha desplegado el código corregido (`index.php`, `producto.php` y `admin_product_delete.php`) en el servidor web de AWS (`/var/www/html/`) usando sudo, resolviendo el problema de no poder borrar productos vendidos.
4. **Seguridad SSL/HTTPS**:
    *   ¡**Certificado SSL activo**! Se ha validado que el HAProxy está usando el certificado de Let's Encrypt para `psicopompo.duckdns.org`.
    *   La web ya es accesible vía HTTPS con el candado verde 🔒.
5. **Auditoría de Seguridad PHP**:
    *   Se ha realizado un análisis de vulnerabilidades (SQLi, XSS, RCE, Broken Auth).
    *   **Mejora implementada**: Se ha blindado el cargador de imágenes (`producto_editar.php`) mediante una lista blanca de extensiones y validación de tipos MIME (`getimagesize`) para prevenir inyecciones de código malicioso (Webshells). ✅

## 🚧 Problemas Actuales
1.  **Conectividad SSH**: Resuelta la duda sobre la ubicación de la llave. La llave `llaves-debian13.pem` se ha devuelto a la raíz del proyecto para facilitar el trabajo.
2.  **Sincronización Git**: Se ha realizado un `git push` exitoso con todo el progreso técnico del día.
3.  **Limitación de Laboratorio**: Las credenciales de S3 para el backup son temporales (AWS Academy), se documentará como una restricción del entorno de desarrollo.
4.  **Análisis de AWS Secrets Manager**: Se ha investigado la integración de seguridad para la gestión de credenciales. Se concluye que, debido a las restricciones de la cuenta de laboratorio de AWS Academy, la implementación técnica se documentará como una mejora crítica para entornos de producción corporativos, justificando el uso actual de archivos de configuración protegidos (`.my.cnf` y PHP) con permisos restrictivos de sistema de archivos.

## 📅 Próximos pasos
- [ ] Pruebas de carga final con **Apache Benchmark** para la memoria.
- [ ] Implementar aviso "Sin stock" visual en el catálogo.
- [ ] Revisión técnica final del código PHP.
- [ ] Limpieza de archivos temporales en AWS.
