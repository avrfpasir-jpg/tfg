# Resumen de Sesión - 24/03/2026

## 📜 Trabajos Finalizados
1. **Infraestructura y Diagrama**:
   - Validación del diagrama de red final en formato Mermaid (`docs/diagrama_red.md`) y revisión del archivo visual SVG.
   - Refleja correctamente el flujo de tráfico actual y el stack de monitorización.

2. **Backups en la Nube (AWS S3)**:
   - Creación del script `/automation/scripts/backup_db.sh`.
   - Modificado para apuntar al bucket: `tfg-sentinel-backups-alex`.
   - Se ha documentado la puesta en marcha en `docs/GUIA_BACKUP_S3.md`.

3. **Corrección de Lógica de Negocio (Soft Delete)**:
   - Problema: No se podían borrar productos del admin si ya habían sido vendidos.
   - Solución: Se implementó un sistema de borrado lógico (columna `activo`).
   - Archivos afectados: `db.sql`, `src/index.php`, `src/producto.php`, `src/actions/admin_product_delete.php`.

## 🚧 Problemas Actuales
- **Acceso SSH Remoto**: Se han reportado errores de `Permission denied` al intentar conectar desde el asistente a las instancias de AWS. Se debe a que la llave `llaves-debian13.pem` se reubicó de la raíz del proyecto a `~/.ssh/`.
- **Actualización de Esquema en Producción**: Pendiente ejecutar el `ALTER TABLE` directamente en MariaDB de AWS para activar la columna `activo`.
- **IP Dynamics**: Actualizadas en `docs/conexiones.txt` (Web: `100.53.188.92` / LB: `13.222.125.54`).

## 📅 Próximos pasos
- [ ] Ejecutar el `ALTER TABLE` en el servidor de Base de Datos.
- [ ] Implementar SSL (HTTPS) con Certbot en el Load Balancer.
- [ ] Commit y Push de todos los cambios de hoy.
